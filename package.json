{
  "name": "flagfill",
  "version": "1.0.0",
  "private": true,
  "description": "FlagFill - colour in the world's flags from memory",
  "type": "module",
  "scripts": {
    "dev": "npx serve .",
    "build:flags": "python3 tools/build_flags.py",
    "build:preview": "node tools/build_preview.js",
    "playtest:live": "node tools/playtest_live.mjs"
  },
  "dependencies": {
    "@upstash/redis": "^1.34.0"
  },
  "devDependencies": {
    "playwright": "^1.63.0"
  }
}
